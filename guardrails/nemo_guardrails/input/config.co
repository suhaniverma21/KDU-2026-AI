define bot allow
  "ALLOW"

define bot deny
  "DENY"

define subflow phase1 prompt injection rail
  $is_injection = execute detect_prompt_injection(text=$user_message)
  if $is_injection
    bot deny
    stop

define subflow allow input
  bot allow
  stop
